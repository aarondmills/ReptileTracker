<%= form_for(@quarterly_report) do |f| %>
  <% if @quarterly_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quarterly_report.errors.count, "error") %> prohibited this quarterly_report from being saved:</h2>

      <ul>
      <% @quarterly_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :market_id %><br />
    <%= f.text_field :market_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
